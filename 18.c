#include <stdio.h>
#define chisel_Dunbara 150
 int main(void)
 {
 int friends = 5;
 int week;
 for (week = 1; (friend - week) * 2 < chisel_Dunbara; week++)
        {
         friends = (friends - week) * 2;
        }
 printf("На %d неделе у профессора Робинсона будет %d друзей", week, friends);
 return 0;
 }
