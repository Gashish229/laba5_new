 #include <stdio.h>
 int main()
 {
   float chisel1, chisel2;
   printf("Введите два числа с плавающей запятой:\n");
   while (scanf("%f %f" , &chisel1, &chisel2))
   {
          printf("Результат: %f\n", ((chislo1 - chislo2) / (chislo1 * chislo2)));
          printf("Введите новые числа (Нечисловое значения для выхода):\n");
   }
   return 0;
 }
